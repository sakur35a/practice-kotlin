spring:
  application:
    name: diary
  cache:
    type: caffeine
    cache-names: diarySlices
    caffeine:
      spec: maximumSize=1000,expireAfterWrite=5m,recordStats
  profiles:
    default: local
